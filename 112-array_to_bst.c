#include "binary_trees.h"

/**
 * array_to_bst - function that builds a Binary Search Tree from an array; if
 * the value of the array is already present in the tree, this value must be
 * ignored.
 * 
 * @array: pointer to the frst element of the array to be converted
 * @size: number of elements in the array
 * Return: pointer to the root node of the created BST, or NULL on failure
 */
bst_t *array_to_bst(int *array, size_t size)
{

}
