#include "binary_trees.h"

/**
 * bst_remove - function that removes a node from a Binary Search Tree. Once
 * located, the node containing a value equal to value must be removed and
 * freed.  If the node to be deleted has two children, it must be replaced with
 * its first in-order successor (not predecessor).
 * 
 * @root: pointer to the root node of the tree where you will remove a node
 * @value: the value to remove in the tree
 * Return: a pointer to the new root node of the tree after removing the
 * desired value.
 */
bst_t *bst_remove(bst_t *root, int value)
{

}
